export interface ISPOptions {
    // Obfuscated personal key
    prf: string;
    // POST flag
    post: boolean;
    // the suggestions flag enables/disables displaying query suggestions in the omnibox.
    suggestions: boolean;
}
